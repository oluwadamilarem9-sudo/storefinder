services:
  - type: web
    name: storefinder
    runtime: python
    plan: free
    buildCommand: pip install -r shopify-lead-finder/requirements.txt
    startCommand: streamlit run shopify-lead-finder/app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
